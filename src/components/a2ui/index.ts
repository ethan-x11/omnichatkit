export const CATALOG_ID = "https://omnichatkit.local/catalog/v1";

import { definitions } from "./definitions";
import { renderers } from "./renderers";

export const catalog = renderers;
export const catalogSchema = definitions;

export { definitions };
